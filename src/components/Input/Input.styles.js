import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  upper_container: {
    marginBottom: 8,
  },
  comment_input: {
    marginTop: 5,
    backgroundColor: '#e0e0e0',
    borderRadius: 5,
    borderWidth:1,
    marginLeft:5,
    marginRight:5,
  },
});
