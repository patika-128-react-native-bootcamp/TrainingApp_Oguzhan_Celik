import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  image: {
    marginTop: 50,
    left: 80,
    width: 200,
    height: 200,
    resizeMode: 'contain',
    tintColor: '#0277bd',
  },
});
