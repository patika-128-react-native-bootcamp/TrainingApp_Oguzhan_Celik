import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container:{
        flex:1,
    },
    mapView:{
        flex:4,
    },
    buttonContainer:{
        marginTop:30,
        flex:1,
    }
});
